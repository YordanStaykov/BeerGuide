import style from './Main.module.scss';

const Main = (props) => {

    return (
        <main className={style.main}>
            <h1>the main page</h1>
        </main>
    );
};

export default Main;